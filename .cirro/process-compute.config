params.mutect_mem = params.mutect_mem ?: 32.GB

process {
  executor = 'awsbatch'

  withName: split_intervals {
    errorStrategy = 'terminate'
    maxRetries = 2
    cpus   = { 2 * task.attempt } 
    memory = 16.GB
  }

  withName: mutect_wrapper {
    errorStrategy = 'terminate'
    maxRetries = 2
    queue        = "${PW_ONDEMAND_JOB_QUEUE}"
    // increase memory each attempt
    memory = { ((params.mem_gb ?: 32) as Integer).GB * task.attempt }   // 16GB, 32GB, 48GB...
    cpus   = 4
  }

  withName: gather_vcfs {
    errorStrategy = 'terminate'
    maxRetries = 2
    cpus   = { 2 * task.attempt } 
    memory = 16.GB
  }
}

aws {
  region = 'us-east-1'
}