process {
  executor = 'awsbatch'
  withName: mutect_wrapper {
    errorStrategy = 'retry'
    maxRetries = 2

    // increase memory each attempt
    memory = { 32.GB * task.attempt }   // 16GB, 32GB, 48GB...
    cpus   = 4
  }
}

aws {
  region = 'us-east-1'
}
